"""mobilerun-testkit — independent regression test suite for mobilerun.

Treats mobilerun as a black-box CLI (public ``mobilerun`` commands and
their exit codes); safe to install and run against any mobilerun setup.
"""

__version__ = "0.1.0"
