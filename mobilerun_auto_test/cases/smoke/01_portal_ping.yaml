id: portal_ping
title: Portal Ping Check
priority: smoke

preconditions:
  - Device connected and mobilerun configured

verify:
  - cmd: "mobilerun ping"
    expect_regex: "pong|ok|success"
    description: "mobilerun ping responds"

timeout: 60
pass_criteria: "mobilerun ping returns successful response"
